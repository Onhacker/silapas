<div class="container-fluid">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <div>
      <h3 class="mb-0"><?= htmlspecialchars($title ?? 'Wallboard', ENT_QUOTES, 'UTF-8') ?></h3>
      <div class="text-muted small"><?= htmlspecialchars($subtitle ?? '', ENT_QUOTES, 'UTF-8') ?></div>
    </div>
  </div>

  <div class="row">
    <!-- Kolom kiri: Monitor (daftar booking & sedang berkunjung) -->
    <div class="col-xl-7 col-lg-7 mb-3">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <?= $monitorHtml /* sudah berupa HTML siap tampil */ ?>
        </div>
      </div>
    </div>

    <!-- Kolom kanan: Dashboard (grafik/rekap) -->
    <div class="col-xl-5 col-lg-5 mb-3">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <?= $dashboardHtml /* sudah berupa HTML siap tampil */ ?>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  /* opsional: biar area di dalam card bisa scroll jika konten tinggi */
  .card-body { max-height: calc(100vh - 220px); overflow:auto; }
</style>
